# Example of dockerfile using nix and julia

A simple Dockerfile that uses nix to define the base python environment that julia uses. If the nixpkgs revision is pinned, this should be reproducible for life.
